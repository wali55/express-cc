<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Put Your Opinion</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3 my-5">
                <div class="card card-body">
                    <h5>Title: <%= poll.title %></h5>
                    <p>Description: <%= poll.description %></p>
                    <div class="my-3">
                        <h5>Result:</h5>
                        <ul class="list-group">
                            <% for (let item of result) { %>
                                <li class="list-group-item">
                                    <p><%= item.name %> ---> <strong><%= item.percentage.toFixed(2) %> %</strong></p>
                                </li>
                            <% } %>
                        </ul>
                    </div>
                    <div class="my-4">
                        <h5>What is your opinion</h5>
                        <form action="/polls/<%= poll._id %>" method="POST">
                            <% for (let option of poll.options) { %>
                                <div class="custom-control custom-radio">
                                    <input type="radio" name="option" id="<%= option._id %>" class="custom-control-input" value="<%= option._id %>">
                                    <label for="<%= option._id %>" class="custom-control-label">
                                        <%= option.name %>
                                    </label>
                                </div>
                            <% } %>
                            <input type="submit" value="Submit Your Opinion" class="btn btn-primary">
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>